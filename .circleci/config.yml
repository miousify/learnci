version: 2.1

jobs:
  build:
    docker:
      - image:  circleci/node:10
    steps:
      - checkout
      - run
          name: First working command
          command: |
            echo "helo world"
            echo "how are you today"